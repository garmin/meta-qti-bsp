# batcam is a derivative of concam.
BASEDISTRO ?= "concam"
require conf/distro/concam.conf

DISTRO_NAME = "batcam"
DISTROOVERRIDES =. "batcam:"

PRODUCT = "batcam"

# Hibernation Support
KERNEL_CMD_PARAMS_append = " resume=/dev/mmcblk0p25 resumewait no_console_suspend=1 "
